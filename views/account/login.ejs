<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %> - Piel de Campeón</title>

    <!-- Librerías de Estilos -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" />
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">

    <!-- Estilos Personalizados para la Página de Login -->
    <style>
        :root {
            --color-primario: #d90429;
            --color-texto-principal: #f5f5f7;
            --color-texto-secundario: #a8a8a8;
        }

        body {
            font-family: 'Poppins', sans-serif;
            margin: 0;
            padding: 0;
        }

        .login-container {
            display: flex;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            background-image: linear-gradient(rgba(18, 18, 18, 0.85), rgba(18, 18, 18, 0.95)), url('/images/hero-main-bg.jpg');
            background-size: cover;
            background-position: center;
            padding: 2rem;
        }

        .login-card {
            max-width: 450px;
            width: 100%;
            background: rgba(30, 30, 30, 0.75);
            backdrop-filter: blur(15px);
            -webkit-backdrop-filter: blur(15px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 1.5rem;
            padding: 2.5rem;
            color: var(--color-texto-principal);
            box-shadow: 0 20px 50px rgba(0,0,0,0.3);
        }

            .login-card h2 {
                font-weight: 800;
            }

            .login-card .form-floating > .form-control {
                background-color: rgba(255, 255, 255, 0.05);
                border-color: rgba(255, 255, 255, 0.2);
                color: #fff;
            }

                .login-card .form-floating > .form-control:focus {
                    background-color: rgba(255, 255, 255, 0.1);
                    border-color: var(--color-primario);
                    box-shadow: none;
                }

            .login-card .form-floating > label {
                color: var(--color-texto-secundario);
            }

            .login-card .form-check-label {
                color: var(--color-texto-secundario);
            }

            .login-card .btn-primary {
                background-color: var(--color-primario);
                border: none;
                font-weight: 600;
                padding: 0.75rem;
                border-radius: 0.5rem;
                transition: background-color 0.2s ease;
            }

                .login-card .btn-primary:hover {
                    background-color: #b80322;
                }

            .login-card .small-links a {
                color: var(--color-texto-secundario);
                text-decoration: none;
                transition: color 0.2s ease;
            }

                .login-card .small-links a:hover {
                    color: #fff;
                    text-decoration: underline;
                }
    </style>
</head>
<body>
    <div class="login-container">
        <div class="login-card">
            <div class="text-center mb-4">
                <h2 class="fw-bold">Bienvenido a Piel de Campeón</h2>
                <p class="text-white-50">Utiliza tu cuenta para ingresar</p>
            </div>

            <section>
                <form action="/account/login" method="post">
                    <div class="text-danger mb-3" role="alert"></div>

                    <div class="form-floating mb-3">
                        <input name="email" id="email" class="form-control" autocomplete="username" aria-required="true" placeholder="nombre@ejemplo.com" />
                        <label for="email" class="form-label"></label>
                        <span class="text-danger"><%= (errores && errores.email) || '' %></span>
                    </div>

                    <div class="form-floating mb-3">
                        <input name="password" id="password" class="form-control" type="password" autocomplete="current-password" aria-required="true" placeholder="contraseña" />
                        <label for="password" class="form-label"></label>
                        <span class="text-danger"><%= (errores && errores.password) || '' %></span>
                    </div>

                    <div class="form-check mb-3">
                        <input class="form-check-input" />
                        <label for="rememberMe" class="form-check-label">
                            Recuérdame
                        </label>
                    </div>

                    <div class="d-grid gap-2">
                        <button type="submit" class="btn btn-primary btn-lg">Ingresar</button>
                    </div>

                    <div class="text-center mt-4 small-links">
                        <a href="/account/recuperar">¿Olvidaste tu contraseña?</a>                    
                        <span class="mx-2 text-white-50">|</span>
                        <a href="/account/registro">Registrar una cuenta nueva</a>
                    </div>
                </form>
            </section>
        </div>
    </div>    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/site.js"></script>
    </body>
</html>
